import styled from "styled-components";

const InteractiveSectionContainer = styled.section`
  display: flex;
  align-items: center;
  justify-content: flex-end;

  > * {
    margin: 0 1rem;
  }
`;

export default InteractiveSectionContainer;
